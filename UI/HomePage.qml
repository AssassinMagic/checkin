import QtQuick
import QtQuick.Controls

Rectangle {
    color: "white"
    anchors.fill: parent

    Column {
        anchors.centerIn: parent
        spacing: 20

        Text {
            text: "🏠 Home Page"
            font.pixelSize: 24
        }
    }
}
