import QtQuick
import QtQuick.Controls 

Rectangle {
    color: "lightgray"
    anchors.fill: parent

    Column {
        anchors.centerIn: parent
        spacing: 20

        Text {
            text: "⚙️ Settings Page"
            font.pixelSize: 24
        }
    }
}
